# Variables are mutable by default, and there is no built-in constant type.
# Reassigning variables is always allowed.
x = 10
print(f"Mutable x is {x}")
x = 20  # Reassigning 'x'
print(f"Mutable x after reassignment is {x}")
